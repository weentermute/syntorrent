<UserControl x:Class="SynTorrent.SessionsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Height="335" Width="310">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32.8"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Height="33" VerticalAlignment="Top">
            <Button x:Name="AddButton" Content="Add..." Click="AddButton_Click"/>
        </StackPanel>
        <ListView x:Name="ConnectionsList" Margin="10,9.8,10,10" Grid.Row="1">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding ConnectionId}" FontWeight="Bold" />
                        <TextBlock Text="{Binding LastMessage}" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </Grid>
</UserControl>
